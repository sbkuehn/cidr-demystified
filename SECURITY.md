# Security

This repo includes scripts that can change network allowlists and access rules.

Recommendations:
- Prefer least privilege and short lived allow rules
- Use `/32` for a single IPv4 host allowlist where supported
- Consider private connectivity for production workloads
