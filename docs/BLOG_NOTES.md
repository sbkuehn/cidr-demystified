# Blog Notes

These scripts support the "CIDR Demystified" blog entry.

Key points the scripts reinforce:
- Your public IP is a single host address, so allowlisting it is typically `/32` in IPv4.
- CIDR is the boundary between network identity and host membership.
- Applying your `/32` to Azure resources is a practical example of the mental model.
